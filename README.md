# Clean Old Files

一个用于清理下载目录中超过指定天数旧文件的Python脚本工具。

## 功能特性

- **智能清理**：自动删除超过指定天数（默认7天）的文件
- **空文件夹清理**：自动删除所有空文件夹
- **文件类型识别**：支持图片、视频、文档、压缩包等多种文件类型
- **安全排除**：自动排除日志文件（.log）和脚本文件本身
- **测试模式**：支持预览模式，不实际删除文件
- **详细日志**：提供详细的清理报告和统计信息
- **跨平台兼容**：支持Windows、macOS、Linux
- **命令行接口**：支持丰富的命令行参数配置

## 安装要求

- Python 3.6 或更高版本
- 无需额外依赖库（使用标准库）

## 使用方法

### 快速启动脚本

为了方便使用，我们提供了平台特定的启动脚本：

- **Windows用户**：双击运行 `clean_old_files.bat` 文件
- **Linux/macOS用户**：运行 `./clean_old_files.sh` （需要先给脚本执行权限：`chmod +x clean_old_files.sh`）

### 基本用法

```bash
python clean_old_files.py [目录路径]
```

### 高级用法

```bash
python clean_old_files.py [选项] [目录路径]
```

### 命令行参数

- `directory`: 要清理的目录路径（默认当前目录）
- `-d, --days`: 保留的天数（默认7天）
- `--dry-run`: 测试模式，不实际删除文件
- `--log-file`: 指定日志文件路径
- `--log-level`: 日志级别（DEBUG, INFO, WARNING, ERROR，默认INFO）
- `--no-log-file`: 不保存日志文件，只输出到控制台

### 使用示例

1. **清理当前目录，删除超过7天的文件**：
   ```bash
   python clean_old_files.py
   ```

2. **清理指定目录，删除超过30天的文件**：
   ```bash
   python clean_old_files.py /path/to/downloads -d 30
   ```

3. **测试模式，预览将要删除的文件**：
   ```bash
   python clean_old_files.py --dry-run
   ```

4. **清理并保存详细日志**：
   ```bash
   python clean_old_files.py --log-file cleanup.log --log-level DEBUG
   ```

## 输出示例

```
文件清理工具
========================================
清理目录: C:\Users\Administrator\Downloads
保留天数: 7
测试模式: False
日志文件: clean_Downloads_20240321.log
日志级别: INFO

============================================================
开始清理旧文件
清理目录: C:\Users\Administrator\Downloads
删除规则: 删除所有超过 7 天的文件
删除规则: 删除所有空文件夹（不考虑时间）
排除规则: 保留.log文件和脚本文件本身
排序方式: 按文件下载到本地的修改时间
测试模式: 否
============================================================
扫描文件中...
扫描完成，共找到 150 个文件
需要删除 45 个超过 7 天的旧文件
释放空间: 2.34 GB

待删除文件列表（从最旧到最新）：
  1. old_file.zip (压缩包, 150.23 MB, 下载时间: 2024-01-01 10:30:15, 已存在: 45天)
  2. temp_video.mp4 (视频, 500.45 MB, 下载时间: 2024-01-02 14:20:30, 已存在: 44天)
  ...

开始删除文件...
已删除: old_file.zip (压缩包, 150.23 MB)
已删除: temp_video.mp4 (视频, 500.45 MB)
...

============================================================
清理完成
删除统计信息:
  - 成功删除文件数量: 45 个
  - 删除失败文件数量: 0 个
  - 释放空间总计: 2.34 GB

详细统计:
  - 平均文件大小: 52.00 MB
  - 删除文件占比: 45/150 (30.0%)
  - 剩余文件数量: 105 个

文件类型统计:
  - 视频: 20 个文件, 1.50 GB
  - 压缩包: 15 个文件, 600.23 MB
  - 图片: 8 个文件, 150.45 MB
  - 文档: 2 个文件, 95.67 MB

总结:
✓ 成功清理了 45 个旧文件
✓ 释放了 2.34 GB 磁盘空间
============================================================
```

## 注意事项

- **备份重要文件**：在运行脚本前，请确保重要文件已备份
- **权限要求**：脚本需要对目标目录及其文件的删除权限
- **测试模式**：首次使用建议先运行测试模式（`--dry-run`）查看将要删除的文件
- **日志文件**：默认会在当前目录生成日志文件，格式为 `clean_{目录名}_{日期}.log`
- **文件判断**：脚本根据文件的修改时间（mtime）判断是否为"旧文件"
- **空文件夹**：会删除所有空文件夹，无论其创建时间

## 更新日志

### v1.1.0 (2026-03-21)
- 添加了 `clean_old_files.bat` 文件，方便Windows用户双击运行
- 添加了 `clean_old_files.sh` 文件，方便Linux/macOS用户运行
- 更新了使用方法，包含脚本文件的说明

## 许可证

本项目采用 MIT 许可证。